spring:
  application:
    name: meritcap-backend

  jackson:
    date-format: dd/MM/yyyy

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}

server:
  servlet:
    context-path: /api
