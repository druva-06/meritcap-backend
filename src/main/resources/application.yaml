spring:
  application:
    name: meritcap-backend

  jackson:
    date-format: dd/MM/yyyy

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  profiles:
    active: dev

server:
  servlet:
    context-path: /api
